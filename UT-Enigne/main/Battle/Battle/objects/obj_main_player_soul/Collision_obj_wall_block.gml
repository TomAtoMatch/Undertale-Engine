/// @description Input your code.
if(position_meeting(x,y+sprite_height/2,obj_wall_block) && angle = 0 && !position_meeting(x,y - sprite_height / 2,obj_wall_draw_other || obj_wall_draw_other))
{
	down_speed_1 = 0;
	can_jump = true;
}else if(position_meeting(x,y-sprite_height/2,obj_wall_block) && angle = 180 && !position_meeting(x,y - sprite_height / 2,obj_wall_draw_other || obj_wall_draw_other))
{
	down_speed_1 = 0;
	can_jump = true;
}else if(position_meeting(x - sprite_width / 2,y,obj_wall_block) && angle = 270 && !position_meeting(x - sprite_width / 2,y,obj_wall_draw_other || obj_wall_draw_other))
{
	down_speed_1 = 0;
	can_jump = true;
}else if(position_meeting(x + sprite_width / 2,y,obj_wall_block) && angle = 90 && !position_meeting(x + sprite_width / 2,y,obj_wall_draw_other || obj_wall_draw_other))
{
	down_speed_1 = 0;
	can_jump = true;
}


